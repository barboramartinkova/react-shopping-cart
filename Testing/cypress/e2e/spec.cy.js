describe('template spec', () => {
  // test that item can be added to cart and user can go to checkout
  it('passes', () => {
    cy.visit('/');
    cy.get('[alt="Cropped Stay Groovy off white"]');
  });

  // test that item can be taken out of the cart

  // test that filter works
});
